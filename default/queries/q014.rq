#id:NXQ_00014
#title:Select persons with their name only if they have NO pet
#comment:Selects subjects with their name but removes solutions. ?person, ?name ?pet are the variables
#comment: note that the ?pet value is bound in the FILTER scope but is not bound in the SELECT scope 
#tags:2 graph patterns,3 variables,FILTER NOT EXISTS
    
select ?person ?name ?pet where {
	?person rdf:type dbo:Person .
	?person dbp:name ?name .
	FILTER NOT EXISTS { ?person tto:pet ?pet}
}
