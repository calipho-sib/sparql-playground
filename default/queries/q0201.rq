#title:Select things that are females
#comment:Selects subjects connected to the literal "female" via the predicate tto:sex
#comment:?thing is the only variable
#tags:1 graph pattern,1 variable
#img:cartoon-female
#color:ex-blue
    
select ?thing where {
  ?thing tto:sex "female" .
}

# Notice that not only the persons
# but also the pets are taken
