#title:Select all things that have a sex
#comment:Selects subjects connected to the object dbo:Person via the predicate rdf:type
#comment:?thing is the only variable
#tags:1 graph pattern,2 variables
#color:ex-blue
    
select ?thing ?sex where {
  ?thing tto:sex ?sex .
}
