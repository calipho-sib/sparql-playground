#id:NXQ_00027
#title:Get the direct sub classes of class Creature
#comment:the single graph pattern matching process finds all the subjects described as a owl:subClassOf tto:Creature .
#tags:1 graph pattern,1 variable

select ?subSpecies   where {
  ?subSpecies owl:subClassOf tto:Creature .
}
